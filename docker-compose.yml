version: '3'
services:
  db:
    image: postgres:10.5
    environment:
      POSTGRES_USER: stackrating
      POSTGRES_PASSWORD: jUBwsv2QeSj5bem
      POSTGRES_DB: stackrating
    volumes:
      - ../data:/var/lib/postgresql/data
  service:
    image: openjdk:8u181-jre-slim
    volumes:
      - ../dist:/opt/stackrating
    depends_on:
      - db
    ports:
      - "4567:4567"
    working_dir: /opt/stackrating
    command: java -jar stackrating-0.0.1-SNAPSHOT.jar keystore.jks ucCU1Ha/rUHCso*U7HCeuHU
